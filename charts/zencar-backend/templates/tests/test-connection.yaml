apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zencar-backend.fullname" . }}-test-connection"
  labels:
    {{- include "zencar-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zencar-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
